# Production Requirements for Symbiote
# Install with: pip install -r requirements_production.txt

# Core Dependencies (already in base requirements)
aiofiles>=23.0.0
pyyaml>=6.0
python-frontmatter>=1.0.0
loguru>=0.7.0
ulid-py>=1.1.0

# Vector Search & Embeddings
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU support
torch>=2.0.0  # Required by sentence-transformers

# Full-Text Search
Whoosh>=2.7.4

# Machine Learning & Analytics
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# NLP & Privacy
spacy>=3.6.0
cryptography>=41.0.0

# Database
duckdb>=0.9.0

# Monitoring & Metrics (optional but recommended)
prometheus-client>=0.17.0

# Testing (for development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Performance Profiling (optional)
memory-profiler>=0.61.0
line-profiler>=4.1.0

# After installing, download spacy model:
# python -m spacy download en_core_web_sm